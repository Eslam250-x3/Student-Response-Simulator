# كيفية تشغيل التول بعد إنشاء الفورمين

بعد إنشاء الاختبارين من `createFlowForm` و `createMCQForm` ووضع الروابط في الـ config، اتبع الخطوات التالية.

---

## 1. التأكد من الإعدادات

| الملف | الحقل | المطلوب |
|-------|-------|---------|
| **config.js** | `formUrl` | رابط فورم MCQ (من `createMCQForm`) |
| **config_flow.js** | `formUrl` | رابط فورم التدفق (من `createFlowForm`) |

- للتجربة بدون إرسال حقيقي: ضع **`dryRun: true`** في config.js  
- للإرسال الفعلي للفورمين: ضع **`dryRun: false`**

---

## 2. (اختياري) نقل بيانات الطالبات لـ Google Sheet

> 💡 لحماية خصوصية البيانات، يُفضل نقل بيانات الطالبات من الكود إلى Sheet خاص.

1. شغّل **`exportStudentsToSheet()`**
2. سيُنشأ Sheet جديد فيه الـ 80 طالبة
3. سيُحفظ الـ Sheet ID تلقائياً في Script Properties
4. بعد التأكد، يمكنك حذف البيانات من `students.js` (اختياري)

---

## 3. تشغيل الاختبارات (مستحسن)

قبل البدء بالمحاكاة، تأكد إن كل الدوال تعمل:

1. اختر الدالة **`runTests`**
2. اضغط **Run** (▶️)
3. لازم كل الاختبارات تنجح (**✅** لكل اختبار)

---

## 4. تشغيل التطبيق القبلي

1. افتح المشروع في [script.google.com](https://script.google.com)
2. من القائمة المنسدلة اختر الدالة **`runPreTest`**
3. اضغط زر **Run** (▶️)
4. أول مرة ستظهر نافذة الصلاحيات → **Review permissions** → اختر حسابك → **Allow**

ما يحدث:
- يتم توليد بروفايلات الـ 80 طالبة
- يتم جدولة مواعيد الردود (MCQ + التدفق)
- يُنشأ Trigger يعمل كل 5 دقائق
- أول ردود MCQ والتدفق تبدأ حسب الجدول

---

## 5. متابعة التقدم

1. اختر الدالة **`checkStatus`**
2. اضغط **Run**
3. في الـ Log ستظهر:
   - عدد الردود المُرسلة (مثلاً: تم 15/80)
   - متوسط MCQ
   - متوسط التدفق (إن كان المقياس مفعّلاً)

شغّل **checkStatus** بين الحين والآخر حتى يصل العدد إلى 80/80.

---

## 6. بعد انتهاء القبلي

عندما يظهر أن التطبيق القبلي اكتمل (80/80):

1. اختر الدالة **`runPostTest`**
2. اضغط **Run**

يبدأ التطبيق البعدي لنفس الـ 80 طالبة على **الفورمين** (MCQ + التدفق) حسب جدول جديد.

---

## 7. بعد انتهاء البعدي

- تظهر رسالة أن المحاكاة اكتملت
- قد يصلك إيميل إشعار
- لتصدير النتائج: شغّل **`exportToSheet()`**

---

## ترتيب التشغيل (ملخص)

| الخطوة | الدالة | متى |
|--------|--------|-----|
| 1 | **exportStudentsToSheet** | مرة واحدة (اختياري — لنقل البيانات لـ Sheet) |
| 2 | **runTests** | قبل أول استخدام (للتحقق من صحة الدوال) |
| 3 | **runPreTest** | مرة واحدة لبدء القبلي |
| 4 | **checkStatus** | بين الحين والآخر لمتابعة التقدم |
| 5 | **runPostTest** | بعد اكتمال القبلي (80/80) |
| 6 | **checkStatus** | لمتابعة البعدي |
| 7 | **exportToSheet** | بعد اكتمال البعدي لتصدير النتائج |

---

## دوال مساعدة

| الدالة | الوظيفة |
|--------|---------|
| **checkStatus** | عرض الحالة والمتوسطات |
| **resumeSimulation** | استئناف محاكاة متوقفة من آخر نقطة |
| **stopSimulation** | إيقاف المحاكاة |
| **resetAll** | مسح كل البيانات والبدء من جديد |
| **runTests** | تشغيل الاختبارات الوحدوية |
| **exportStudentsToSheet** | نقل بيانات الطالبات لـ Google Sheet |
| **debugProperties** | عرض أحجام البيانات المخزنة |

---

## القائمة المخصصة (في Google Sheets)

عند فتح الشيت المرتبط، ستظهر قائمة **"محاكاة الاختبار"** تحتوي على:
- تشغيل القبلي / البعدي
- استئناف المحاكاة
- حالة المحاكاة
- تصدير النتائج / بيانات الطالبات
- إيقاف / إعادة تعيين
- عرض خصائص التخزين

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| المحاكاة توقفت فجأة | شغّل **`resumeSimulation()`** — يستكمل من آخر نقطة |
| إيقاف المحاكاة فوراً | شغّل **`stopSimulation()`** |
| مسح كل شيء | شغّل **`resetAll()`** ثم **`runPreTest()`** |
| خطأ في البداية | شغّل **`runTests()`** أولاً للتأكد من صحة الكود |
| حد التخزين 9KB | البيانات الكبيرة تُقسّم تلقائياً (لا تحتاج تدخل) |
