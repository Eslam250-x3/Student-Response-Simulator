# 📘 مرشد تشغيل نظام محاكاة الاستجابات

دليل خطوة بخطوة لتشغيل السكربتات من توليد البيانات حتى إرسال الفورمات وتوليد دفتر المهام.

---

## 📋 نظرة عامة على المسار

```
1. توليد simulation_data.json (بايثون)
         ↓
2. رفع JSON إلى Google Drive + إعداد submit_from_json
         ↓
3. إرسال القبلي (مجدول أو فوري)
         ↓
4. بعد انتهاء القبلي → إرسال البعدي
         ↓
5. توليد دفتر المهام Excel (بايثون)
```

---

## 🔧 المتطلبات المسبقة

| الأداة | الاستخدام |
|--------|-----------|
| **Python 3.8+** | تشغيل `generate_simulation.py` و `generate_tasks_gradebook.py` |
| **pip** | تثبيت المكتبات |
| **حساب Google** | Drive + Forms + Apps Script |
| **ملف الإعداد** | `extracted_config.json` (أو يستخدم الإعدادات الافتراضية) |

تثبيت مكتبات البايثون:

```bash
pip install numpy scipy pandas openpyxl
```

---

## المرحلة 1: توليد بيانات المحاكاة (simulation_data.json)

### الخطوات

1. افتح **Command Prompt** أو **PowerShell** في مجلد المشروع:
   ```bash
   cd "e:\New folder (6)"
   ```

2. شغّل سكربت التوليد:
   ```bash
   python generate_simulation.py
   ```
   أو مع seed ثابت (لإعادة نفس النتائج):
   ```bash
   python generate_simulation.py --seed 42
   ```
   أو مع ملف إعداد مخصّص:
   ```bash
   python generate_simulation.py --config extracted_config.json
   ```

3. تأكد من ظهور الملف:
   - **الناتج:** `simulation_data.json` في نفس المجلد.
   - يحتوي على 96 طالباً (استجابات قبلي/بعدي MCQ + Flow).

---

## المرحلة 2: ربط Google Apps Script بالبيانات

### 2.1 رفع الملف إلى Google Drive

1. افتح [Google Drive](https://drive.google.com).
2. ارفع ملف **simulation_data.json** (اسحبه أو File → Upload).
3. انقر يميناً على الملف → **نشارك** → تأكد أن من يمكنه المشاهدة هو **أنت** أو الحساب الذي يشغّل السكربت.
4. انقر يميناً على الملف → **الحصول على الرابط** → انسخ **معرف الملف (File ID)**.
   - الرابط يبدو مثل: `https://drive.google.com/file/d/XXXXXXXXXX/view`
   - الـ **File ID** هو الجزء `XXXXXXXXXX` بين `/d/` و `/view`.

### 2.2 إعداد submit_from_json في Google Apps Script

1. افتح مشروع **Google Apps Script** الذي فيه `submit_from_json.js`.
2. في أعلى ملف **submit_from_json.js** عدّل:

   ```javascript
   var SIMULATION_FILE_ID = "XXXXXXXXXX";  // ← File ID من Drive
   var MCQ_FORM_URL = "https://docs.google.com/forms/d/..../edit";   // رابط فورم MCQ
   var FLOW_FORM_URL = "https://docs.google.com/forms/d/..../edit";   // رابط فورم التدفق
   ```

3. (اختياري) لو تريد تخزين الـ File ID في الخصائص بدل الكود:
   - في المحرر: **المشروع (العتاد)** ⚙️ → **خصائص مشروع السكربت** → أضف مفتاح `SIMULATION_FILE_ID` وقيمة = File ID.

---

## المرحلة 3: إعداد نماذج Google (Forms)

قبل أي إرسال، تحقق من التالي:

| الإعداد | المطلوب |
|---------|---------|
| **جمع عناوين البريد** | مفعّل (إدخال المجيب)، وليس "Verified" فقط |
| **التقييد برد واحد** | **معطّل** (لأن السكربت يرسل عشرات الردود من نفس الحساب) |
| **ترتيب الخيارات عشوائي** | **معطّل** (لأن الإجابات في JSON مرتبة حسب ترتيب الخيارات في الفورم) |

---

## المرحلة 4: تشغيل الإرسال إلى الفورمات

لديك **طريقتان**.

### الطريقة أ: إرسال فوري (كل شيء دفعة واحدة)

مناسبة للتجربة السريعة أو عدد طلاب محدود.

1. في Apps Script: من القائمة اختر الدالة **`submitAllFromJSON`** ثم **تشغيل**.
2. أول مرة: وافق على صلاحيات الحساب (Drive + Forms).
3. السكربت يرسل لكل الطلاب: MCQ قبلي → MCQ بعدي → Flow قبلي → Flow بعدي (مع تأخير عشوائي بين الطلبات).
4. إذا توقف: شغّل `submitAllFromJSON` مرة أخرى؛ يتم الاستكمال من آخر نقطة محفوظة.

**دوال مساعدة:**
- **`previewData`** — معاينة ملخص البيانات دون إرسال.
- **`resetSubmitState`** — مسح حالة الاستكمال لو أردت البدء من الصفر.

---

### الطريقة ب: إرسال مجدول (Smart Queue — أوقات نهارية + فجوة ساعتين)

مناسبة للمحاكاة الواقعية: كل طالب له MCQ ثم Flow بعده بحوالي ساعتين، في أوقات نهارية فقط.

#### خطوة 1: تشغيل القبلي

1. من القائمة في Apps Script اختر **`runPreTestJSON`** ثم **تشغيل**.
2. السكربت يبني الطابور (MCQ قبلي + Flow قبلي لكل الطلاب) ويجدول الإرسال كل 5 دقائق.
3. اترك السكربت يعمل؛ الـ Trigger يرسل تلقائياً حسب الجدول.

#### خطوة 2: متابعة التقدم

- من القائمة شغّل **`checkJSONStatus`** لمعرفة: كم تم إرساله، كم متبقي، أقرب موعد قادم.

#### خطوة 3: بعد اكتمال القبلي

- عندما يظهر أن القبلي اكتمل (أو في السجلات: "التطبيق القبلي اكتمل"):
- شغّل **`runPostTestJSON`**.
- السكربت يستبعد الـ 16 متسرباً تلقائياً ويرسل البعدي لـ 80 طالباً فقط.

#### دوال إدارة المجدول

| الدالة | الوظيفة |
|--------|----------|
| **`runPreTestJSON`** | بدء القبلي (طابور ذكي) |
| **`runPostTestJSON`** | بدء البعدي (بعد اكتمال القبلي) |
| **`checkJSONStatus`** | عرض التقدم والمتبقي |
| **`resetJSONState`** | إيقاف الـ Trigger ومسح الحالة والطابور |

---

## المرحلة 5: توليد دفتر درجات المهام (Excel)

بعد وجود ملف **simulation_data.json** (نفس الملف المستخدم في الإرسال):

1. في مجلد المشروع:
   ```bash
   cd "e:\New folder (6)"
   python generate_tasks_gradebook.py
   ```

2. خيارات إضافية:
   ```bash
   python generate_tasks_gradebook.py --input simulation_data.json --output tasks_gradebook.xlsx
   python generate_tasks_gradebook.py --seed 42
   ```

3. **الناتج:** ملف **tasks_gradebook.xlsx** في نفس المجلد (أو المسار الذي حددته في `--output`).
   - صف لكل طالب، مع درجات M1–M5 وأوقات التسليم والبونص والمجموع.
   - المتسربون يظهرون بتسليم M1 و M2 فقط والباقي "لم يتم التسليم".

---

## 📊 القائمة المخصصة في Apps Script

إذا ربطت السكربت بملف **Google Sheet** وفتحته، تظهر قائمة **"📤 إرسال المحاكاة"**:

- **معاينة البيانات** → `previewData`
- **مجدول — Smart Queue:** تشغيل القبلي / البعدي، حالة المحاكاة، إيقاف وإعادة تعيين
- **فوري:** إرسال الكل، MCQ فقط، Flow فقط، مسح حالة الإرسال الفوري

---

## 🔗 الربط بين النتائج (كل طالب مسقّاة نتائجه)

| المصدر | مفتاح الربط |
|--------|-------------|
| الفورم (قبلي/بعدي) | **الإيميل** (نفس `student.email` من JSON) |
| دفتر المهام Excel | **كود الطالب** (`student.id`) |

نفس الـ **simulation_data.json** يستخدمه الإرسال ودفتر المهام؛ لذلك الإيميل في الفورم يطابق الطالب، وكود الطالب في Excel يطابق نفس الطالب في الـ JSON — ويمكن ربط الفورم مع Excel عبر الـ JSON (إيميل ↔ id).

---

## ⚠️ استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| "ضع SIMULATION_FILE_ID" | ضع File ID في أعلى `submit_from_json.js` أو في Script Properties |
| "فشل تحميل الملف" | تأكد أن الملف في Drive وأن الحساب له صلاحية مشاهدة الملف |
| "فشل فتح فورم MCQ/Flow" | تحقق من صحة `MCQ_FORM_URL` و `FLOW_FORM_URL` (رابط يبدأ بـ `https://docs.google.com/forms/d/...`) |
| الفورم يرفض الرد الثاني | أزل "التقييد برد واحد" من إعدادات النموذج |
| الدرجات لا تطابق الخيارات | أغلِق "ترتيب الخيارات عشوائي" في الفورم |
| "لازم التطبيق القبلي يخلص الأول" | شغّل `runPostTestJSON` فقط بعد اكتمال القبلي (راجع `checkJSONStatus`) |

---

## 📁 ملخص الملفات

| الملف | الوظيفة |
|-------|----------|
| **generate_simulation.py** | يولّد `simulation_data.json` (96 طالب، قبلي/بعدي) |
| **simulation_data.json** | مصدر موحد للإرسال ودفتر المهام |
| **submit_from_json.js** | يقرأ JSON من Drive ويرسل إلى فورمات MCQ و Flow |
| **generate_tasks_gradebook.py** | يقرأ JSON ويولّد `tasks_gradebook.xlsx` |
| **students.js** | بيانات الطالبات (للشيت/للمشروع؛ الإرسال يعتمد على JSON) |

---

*آخر تحديث: فبراير 2026*
